{
  "name": "project-planner",
  "version": "0.1.0",
  "private": true,
  "description": "Single-file HTML project planning application",
  "scripts": {
    "build": "python3 build.py",
    "test": "node --test"
  }
}
